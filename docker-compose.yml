version: '3.9'
services:
  api:
    build: .
    container_name: seu-cantinho-api
    environment:
      - PORT=3333
      - DATABASE_FILE=/app/data/database.sqlite
    ports:
      - '3333:3333'
    volumes:
      - api-data:/app/data
    restart: unless-stopped
volumes:
  api-data:
